# Don't check in secrets in .env, use AWS Parameter Store
# .env is for any values you need for local development and don't want to share with project
.env

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
.DS_Store
aws/.vscode/launch.json
.vscode/settings.json
.history
.aider*
